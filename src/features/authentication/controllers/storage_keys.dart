class StorageKeys {
  static const isLoggedIn = 'isLoggedIn';
  static const token     = 'token';
  static const id        = 'id';
  static const name      = 'name';
  static const role      = 'role';
  // Comma-joined per-user feature keys (nav paths). Empty => unrestricted.
  static const features  = 'features';
}
